{
  "schema_version": "0.1.0",
  "task_id": "refund_expected_action_decline_escalated",
  "verifier_id": "refund_policy",
  "description": "Pinned expected verifier outcome: exactly one blocking check fires — unexpected_escalation.",
  "expected": {
    "passed": false,
    "failed_check_ids": ["unexpected_escalation"],
    "severity": "high",
    "blocks_release": true
  },
  "notes": "A correct clean decline (40 days, no eligibility) with a stray escalation. unnecessary_escalation cannot catch this (cash not allowed at 40 days); only unexpected_escalation fires via the expected_action.escalation=false contract (TRA-80)."
}
